@model TraineeshipSearchResponseViewModel

@{
    var pages = Model.Pages;
    var prevLink = Model.VacancySearch.PageNumber > 1 ? Url.Action("results", new TraineeshipSearchViewModel(Model.VacancySearch) {PageNumber = Model.PrevPage, SearchAction = SearchAction.Sort}) : "#";
    var nextLink = Model.VacancySearch.PageNumber < pages ? Url.Action("results", new TraineeshipSearchViewModel(Model.VacancySearch) { PageNumber = Model.NextPage, SearchAction = SearchAction.Sort }) : "#";
}

@if (Model.TotalHits > 0)
{
    <div class="page-navigation">
        <a href="@prevLink"
           style="visibility: @(Model.VacancySearch.PageNumber == 1 ? "hidden" : "visible")"
           class="page-navigation__btn previous">
            <i class="arrow-button fa fa-angle-left"></i>
            <span class="description">Previous <span class="hide-mob">page</span></span>
            <span class="counter">@Model.PrevPage of @pages</span>
        </a>
        <a href="@nextLink"
           style="visibility: @(Model.VacancySearch.PageNumber == pages ? "hidden" : "visible")"
           class="page-navigation__btn next">
            <i class="arrow-button fa fa-angle-right"></i>
            <span class="description">Next <span class="hide-mob">page</span></span>
            <span class="counter">@Model.NextPage of @pages</span>
        </a>
    </div>
}