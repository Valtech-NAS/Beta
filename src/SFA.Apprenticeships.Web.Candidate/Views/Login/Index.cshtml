@model LoginViewModel

@{
    ViewBag.Title = "Apprenticeships - Sign in or create an account";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Html.Partial("ValidationSummary", ViewData.ModelState)

<div class="grid-wrapper">
    <div class="grid grid-1-2">
        @using (Html.BeginForm("Index", "Login", FormMethod.Post))
        {
            <fieldset>
                <legend class="heading-large">Sign in</legend>
                <p>
                    Sign in or
                    @Html.ActionLink("create an account", controllerName: "Register", actionName: "Index").
                </p>

                @Html.FormTextFor(
                    m => m.EmailAddress,
                    containerHtmlAttributes: new { @class = "form-group-compound" },
                    controlHtmlAttributes: new { @class = "form-control-3-4", type = "email", autofocus = "true" })

                <div class="form-group">
                    <a id="password"></a>
                    @Html.LabelFor(m => m.Password, new { @class = "form-label" })
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control form-control-3-4" })
                    @Html.ValidationMessageFor(m => m.Password, null)

                    <a class="form-hint" href='@Url.Action("ForgottenPassword", "Register")'>Forgotten password?</a>
                </div>

                <div class="form-group">
                    <button class="button" id="sign-in-button">Sign in</button>
                </div>
            </fieldset>
        }
    </div>
    <div class="grid grid-1-2">
        <h2 class="heading-large">Create an account</h2>
        <div class="text">
            <p>
                If you’ve already applied for an apprenticeship you can still search for it by <a href="https://apprenticeshipvacancymatchingservice.lsc.gov.uk/navms/Forms/Candidate/CandidatesLogin.aspx" rel="external">signing in</a> to your old account.
            </p>
            <p>
                This is a new service. You must create an account if you’ve not used it before.
            </p>
        </div>
        

        @Html.ActionLink("Create an account", "Index", "Register", null, new { id = "create-account-link" })
    </div>
</div>
